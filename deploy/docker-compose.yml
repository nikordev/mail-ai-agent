name: mail-ai-agent
include:
  - docker-compose.common.yml
services:
  app:
    image: "ghcr.io/nikordev/mail-ai-agent:${APP_VERSION}"
    volumes:
      - ./.data/app:/data
    depends_on:
      ollama:
        condition: service_healthy
    env_file:
      - .env
    restart: unless-stopped